apiVersion: v2
name: external-secrets-04-aws-upstream
description: A Helm chart for Kubernetes

type: application

version: 0.1.0

appVersion: "1.16.0"

dependencies:
  - name: external-secrets
    version: "0.10.3"
    # verion: "VERSION"
    repository: https://charts.external-secrets.io
