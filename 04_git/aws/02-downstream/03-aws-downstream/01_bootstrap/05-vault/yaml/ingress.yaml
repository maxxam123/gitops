---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vault-ingress-03-aws-downstream
  namespace: vault
spec:
  ingressClassName: external-nginx
  rules:
    - host: 03-aws-downstream-vault.panwang.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vault-03-aws-downstream-ui
                port:
                  number: 8200
