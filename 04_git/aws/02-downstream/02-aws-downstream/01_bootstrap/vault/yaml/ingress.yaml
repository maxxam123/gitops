---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-vault-ingress-02-downstream
  namespace: vault
spec:
  ingressClassName: external-nginx
  rules:
    - host: 02-downstream-vault.panwang.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vault-02-downstream-ui
                port:
                  number: 8200
